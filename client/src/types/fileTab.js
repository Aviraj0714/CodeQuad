import { FileSystemItem } from "./file";

const FileTabContext = {
    activeFile: null,
    setActiveFile: (file) => {
        // Function to set activeFile, implement your logic here
    },
    changeActiveFile: (fileId) => {
        // Function to change the activeFile based on fileId, implement your logic here
    }
};

export { FileTabContext };
